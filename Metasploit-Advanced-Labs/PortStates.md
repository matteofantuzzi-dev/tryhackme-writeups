Stato:
-Open -> il servizio accetta connessioni e di conseguenza si possono cercare exploit per quel servizio.
-Closed -> L'host risponde ma non c'è servizio e quindi si può ignorare
-Filtered -> Un firewall sta bloccando i pacchetti e in questo caso si possono tentare tecniche di evasione o cambiare porta
